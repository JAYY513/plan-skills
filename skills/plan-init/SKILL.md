---
name: plan-init
description: 为项目初始化或升级分层计划体系（vibe coding 防计划混乱）。当用户说"初始化计划体系""开始新项目规划""帮我建立 SPEC/ROADMAP/TASKS""setup 计划文档""重新 init""更新计划模板"，或在新项目根目录准备开始开发时使用。会创建 SPEC.md、ROADMAP.md、INBOX.md、TASKS.md、FINDINGS.md 五个状态文件，并向 AGENTS.md 注入计划纪律段落。全新初始化每个项目只运行一次；文件已存在时进入升级评估（升级 / 重置 / 不动三选一），任何情况下不覆盖用户数据。
disable-model-invocation: true
---

# plan-init：项目计划体系初始化与升级

## 目标

在项目根目录建立四层结构：状态文件（SPEC/ROADMAP/INBOX/TASKS/FINDINGS）+ 纪律（AGENTS.md）。
状态文件管"现在是什么"，AGENTS.md 管"永远不许怎样"。

各文件分工：
- SPEC.md = 锚：项目是什么、不做什么、技术选型，基本不动
- ROADMAP.md = 路标：3~5 个里程碑 + 已完成里程碑的产出存档
- TASKS.md = 施工图：仅当前里程碑的任务，完成的待归档不删
- INBOX.md = 停车场：新想法、待裁决项
- FINDINGS.md = 知识库：调研结论、重要发现、失败尝试，跨会话不丢；「索引 + 热区」两层，平时只读索引，历史条目全文由 plan-review 分诊进 FINDINGS.archive.md

## 执行步骤

1. 检查项目根目录是否已存在 SPEC.md / ROADMAP.md / TASKS.md / INBOX.md / FINDINGS.md，按结果分流：
   - **全部不存在** → 走「首次初始化」（步骤 2~6）
   - **任一存在** → 项目已初始化过，按规则不覆盖任何文件。向用户列出已存在文件，并简要对比现有文件与 `assets/templates/` 最新模板的结构差异（缺哪些 section、头部纪律块是否变过），给出三个选项，等用户选定后再动手，未选定前禁止写入：
     - **升级**（推荐）：按「升级合并规则」把现有文件结构对齐最新模板——结构向新模板看齐，数据向现有文件看齐；完成后走步骤 4~6
     - **重置**：需用户显式确认。旧五文件整体移入 `.planning/legacy-init/<YYYY-MM-DD>/`（备份不删除；`.planning/*` 被 gitignore 时加一行 `!.planning/legacy-init/` 例外，让备份随 `.planning/done/` 一样入库），然后重走「首次初始化」全部步骤（AGENTS.md 的旧「计划纪律」段由步骤 4 的幂等替换自动更新，无需单独处理）
     - **不动**：什么都不改，直接结束

### 首次初始化

2. 向用户提 4 个问题（一次性问完，不要逐个问）：
   - 这个项目最终要交付什么？（1~3 条，写进 SPEC 和 ROADMAP）
   - 第一个里程碑想做成什么样、怎么算验收通过？
   - 技术栈是否已确定？（确定则写进 SPEC，不确定则在 SPEC 标记"待定"）
   - 这些交付里哪些是 MVP 必备、哪些放 P0/P1？
3. 将 `assets/templates/` 下 5 个模板（SPEC/ROADMAP/INBOX/TASKS/FINDINGS）复制到项目根目录，用用户的回答填充占位内容；第 4 个问题的回答归入 ROADMAP.md「范围分层」的 MVP / P0 / P1 各桶。
4. 注入 AGENTS.md（幂等，锚点为 `## 计划纪律`）：
   - 文件不存在或无锚点 → 新建 / 追加 `assets/templates/AGENTS.snippet.md` 内容到文件末尾，保持用户原有内容不动
   - 已有锚点 → 用最新 snippet 整体替换「锚点起至下一个 `##` 同级标题（或文件末尾）」之间的整段，其余内容不动
   - 任何情况下不允许出现两份「计划纪律」段
5. （可选）检测 hook 平台：项目根存在 `.claude/`、`.codex/`、`.opencode/` 目录，或用户声明所用平台 → 询问是否安装对应 hooks（从技能包 `hooks/<platform>/` 复制脚本并按该平台 README 写入配置）。无匹配平台 → 跳过不报错，并向用户说明：无 hook 的平台靠 AGENTS.md 纪律达到等价行为，只是强度较弱（靠自觉而非强制）。
6. 完成后输出一段简短的使用说明：每天看什么、新想法怎么处理、调研结论落哪里、什么时候跑 plan-review（计划变更门）。

### 升级（用户选「升级」时）

跳过步骤 2~3（数据都在现有文件里，不需要重新提问），先按「升级合并规则」逐文件合并，然后执行步骤 4（AGENTS.md 幂等替换——snippet 本身可能已更新）、5、6。

## 升级合并规则

以当前技能包 `assets/templates/` 为目标结构，逐文件对比，合并单元 = 文件头部 `>` 纪律块 + `##` 二级 section。总原则：**结构向新模板看齐，数据向现有文件看齐**。

| 内容类型 | 判定 | 处理 |
|---|---|---|
| 纪律指令 | 头部 `>` 纪律块；说明性 section（教"怎么用这个文件"，如 FINDINGS 的「读法纪律」「条目格式」） | 以新模板为准：先展示 diff，用户确认未定制过才替换；定制过则保留用户版，并在 INBOX.md 记一条待裁决 |
| 用户数据 | 数据性 section（装实际内容：范围分层、里程碑、任务队列、索引、热条目、选型表、变更日志等） | 原样保留，永不覆盖 |
| 新增结构 | 新模板有、现有文件缺的 section / 纪律行 | 补进对应位置，内容用占位 |
| 废弃结构 | 现有文件有、新模板已删除的 section | 保留不删（完成历史不删除），加一行注释标记「新版模板已废弃」 |

- 每个文件落盘前展示三类清单——将新增 / 将替换（附 diff）/ 保持不动——用户确认后执行；逐文件确认，不要一次性全改
- 判不清是"指令"还是"数据" → 按用户数据处理（宁可少改，不可误改）
- 全部完成后在 SPEC.md「变更日志」追加一行（日期 + "模板结构升级" + 一句话原因）

## 原则（向用户解释时引用）

- 任务层乱是正常的，不需要治；锚（SPEC）和路标（ROADMAP）不许随便动
- 新想法一律先进 INBOX.md，禁止当场改 ROADMAP.md / SPEC.md
- 调研结论一律落 FINDINGS.md，禁止只留在对话里
- 每条信息只有一个家，其他文件只引用不复制
- 完成历史不删除：任务归档到 ROADMAP 里程碑下，随时能回答"这个阶段做了什么"
- 计划维护时间红线：每天 ≤10 分钟，每周 ≤30 分钟
